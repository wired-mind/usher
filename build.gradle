allprojects {
    apply plugin: 'eclipse'
    apply plugin: 'idea'

    repositories {
        maven { url 'http://ec2-54-198-158-92.compute-1.amazonaws.com/content/groups/public/' }
        mavenLocal()

        flatDir {
            dirs 'lib'
        }
    }

}

group = 'io.cozmic'

subprojects {


    defaultTasks = ['assemble']

}

task wrapper(type: Wrapper) {
    gradleVersion = '2.0'
}